# =============================================================================
# EPANNs Finetuned Binary - AudioSet-EV v1 - Benchmark Configuration
# =============================================================================
# Benchmark EPANNs finetuned on AudioSet-EV v1 (binary task) across all datasets
#
# Training details:
#   - Dataset: AudioSet_EV_v1_2025
#   - Task: Binary Emergency Vehicle classification
#   - Best epoch: 6 (F1=0.8565)
#
# Usage:
#   python main_benchmark.py --config benchmark_configs/epanns_finetuned_binary_AS-EV_v1.yaml
# =============================================================================

# Model Configuration
model:
  name: "epanns"
  checkpoint_type: "finetuned"
  checkpoint_path: "./checkpoints/binary_EV/epanns_finetune_fixedLR_AS-EV_v1/epoch=006_val_f1=0.8565.ckpt"

# Benchmark Configuration
benchmark:
  batch_size: 32
  limit_test_batches: null
  datasets_to_test: []
  output_dir: "./benchmark_results"
