# =============================================================================
# CED Finetuned Binary - AudioSet-EV v1 - Benchmark Configuration
# =============================================================================
# Benchmark CED finetuned on AudioSet-EV v1 (binary task) across all datasets
#
# Training details:
#   - Dataset: AudioSet_EV_v1_2025
#   - Task: Binary Emergency Vehicle classification
#   - Best epoch: 0 (F1=0.8750)
#
# Usage:
#   python main_benchmark.py --config benchmark_configs/ced_finetuned_binary_AS-EV_v1.yaml
# =============================================================================

# Model Configuration
model:
  name: "ced"
  checkpoint_type: "finetuned"
  checkpoint_path: "./checkpoints/binary_EV/ced_finetune_fixedLR_AS-EV_v1/epoch=000_val_f1=0.8750.ckpt"

# Benchmark Configuration
benchmark:
  batch_size: 32
  limit_test_batches: null
  datasets_to_test: []
  output_dir: "./benchmark_results"
