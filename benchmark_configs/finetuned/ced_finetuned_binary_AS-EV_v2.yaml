# =============================================================================
# CED Finetuned Binary - AudioSet-EV v2 - Benchmark Configuration
# =============================================================================
# Benchmark CED finetuned on AudioSet-EV v2 (binary task) across all datasets
#
# Training details:
#   - Dataset: AudioSet_EV_v2PANNs_2020
#   - Task: Binary Emergency Vehicle classification
#   - Best epoch: 3 (F1=0.9682)
#
# Usage:
#   python main_benchmark.py --config benchmark_configs/ced_finetuned_binary_AS-EV_v2.yaml
# =============================================================================

# Model Configuration
model:
  name: "ced"
  checkpoint_type: "finetuned"
  checkpoint_path: "./checkpoints/binary_EV/ced_finetune_fixedLR_AS-EV_v2/epoch=003_val_f1=0.9682.ckpt"

# Benchmark Configuration
benchmark:
  batch_size: 32
  limit_test_batches: null
  datasets_to_test: []
  output_dir: "./benchmark_results"
